void main() {
	synchronized int x = 4;
	synchronized stateInfo.status = "can't connect (%s)".format(addr);
	return;
	synchronized (foo) {}
	int x;
}

synchronized class Foo {
	synchronized SomeFunction!(int) foo() {}
	synchronized int foo();
}